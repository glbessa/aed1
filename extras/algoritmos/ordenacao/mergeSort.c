#include <stdio.h>
#include <stdlib.h>
#include <math.h>

#define LEN 6

int main() {
    int array[LEN] = {5, 2, 6, 1, 4, 3};

    for (int i = 0; i < ceil(LEN / 2); i++) {
        
    }

    return 0;
}