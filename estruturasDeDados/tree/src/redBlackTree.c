#include "redBlackTree.h"
#include <stdlib.h>
#include <stdio.h>

/*
redBlackTree_t *CreateTree() {
    redBlackTree_t *tree = (redBlackTree_t *) malloc(sizeof(redBlackTree_t));
    if (!tree) {
        perror("Error when creating tree");
        exit(-1);
    }

    tree->value = NULL;
    tree->parent = NULL;
    tree->left = NULL;
    tree->right = NULL;

    return tree;
}

void Insert(redBlackTree_t *tree, void *value) {
    
}

void *Search(redBlackTree_t *tree, void *value) {

}

void Delete(redBlackTree_t *tree, void *value) {

}
*/